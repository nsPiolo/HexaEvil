#!/usr/bin/env bash
# Hook Git versionné — voir CONTRIBUTING.md §1 pour l'installation
set -e

ROOT_DIR="$(git rev-parse --show-toplevel)"
COMMIT_MSG_FILE="$1"

if ! command -v node >/dev/null 2>&1; then
  exit 0
fi

node "$ROOT_DIR/tools/hooks/check-commit-msg.mjs" "$COMMIT_MSG_FILE"
